/project
│
├── index.html                # Login page
├── signup.html               # Sign Up page
├── dashboard.html            # Student Dashboard
├── registered.html           # Registered Organization page
├── student-form.html         # Student Organization Registration Form
│
├── /css
│   ├── base.css              # Global styles (font, background, variables)
│   ├── components.css        # Reusable UI components (buttons, inputs, labels)
│   ├── dashboard.css         # Layout + sidebar + main container (shared by pages)
│   ├── login.css             # Login-only styles
│   ├── signup.css            # Signup-only styles
│   └── student-form.css      # Form-only styles (upload, spacing, submit button)
│
├── /js
│   └── student-form.js       # Upload button + file list UI
│
└── /assets
    ├── /logos
    │   └── logo.png          # Your app / school logo
    │
    ├── /icons
    │   └── (future icons)    # Status icons, nav icons, etc.
    │
    └── /files
        └── (future uploads)  # Admin-uploaded/downloadable forms
